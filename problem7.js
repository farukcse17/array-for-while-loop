// Create an array for all the mobile phones. display all the elements of them by using while loops;

var mobiles = ['samsungButton', 'nokiya', 'huweai', 'infinix'];
var count = 0;
while(count < mobiles.length){
    var mobile = mobiles[count];
    console.log(mobile);
    count++;
}