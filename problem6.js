// create an array for all the loops that you have learned last few days. display them output.

var learns = ['html', 'css', 'css3', 'daisyUI', 'tailwind', 'figmaConversion'];
for(var i = 0; i < learns.length; i++){
    var learn = learns[i];
    console.log(learn);
}

